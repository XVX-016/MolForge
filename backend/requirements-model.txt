# Heavy ML/Model dependencies - installed separately for better Docker caching
# These rarely change, so Docker can cache this layer

##############################
# PyTorch (CPU-only for smaller size)
##############################
# Using CPU-only wheels to reduce size from 800MB to ~120MB
torch==2.4.1+cpu
torchvision==0.19.1+cpu
torchaudio==2.4.1+cpu

##############################
# ML/AI Libraries
##############################
transformers==4.41.2
tokenizers==0.19.1
onnxruntime==1.19.2

##############################
# Numerical Computing
##############################
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.4

