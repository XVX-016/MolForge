{
    "name": "@biosynth/backend",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "python -m uvicorn app:app --reload --host 127.0.0.1 --port 8000",
        "start": "python -m uvicorn app:app --host 0.0.0.0 --port 8000"
    }
}